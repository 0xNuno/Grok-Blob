# Build commands

See package.json packaging scripts and scripts/zipapp.mjs (dir|full).
macOS out: ~/Library/Caches/GrokBlob/out (BLOB_OUT overrides).
Copy Blob.app to ~/Applications or /Applications.
