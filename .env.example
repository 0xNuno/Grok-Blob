# Optional local shortcut for first boot. The app stores URL + token in
# ~/Library/Application Support/Blob/settings.json (Keychain for the token).
# Copy to .env and fill in, or use the first-run sheet instead.
# Never commit a real .env.

GROKBOT_GATEWAY_URL=
SAND_GATEWAY_TOKEN=
